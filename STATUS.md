# Profusion Pipeline Status

## Current Project State: Profusion split from education/content engine; M7 locked; M7.5 receipt slice complete; M8 outcome observations in progress

As of 2026-05-17, Profusion is the B2B workflow-trust project. The real education/content engine, MoneyPrinterTurbo/MoneyPrinterV2 lineage, Substack package work, source packs, voice guides, and owned-media drafts now belong to `/home/kyle/attention-media-lab`. Profusion may keep a sanitized AI-assisted media/content demo only to explain receipt mechanics.

As of 2026-05-08, M6 remains functionally closed and M7 is locked as the
internal operator cockpit milestone. M8 has now started as manual workflow
outcome observation plus comparison loops. The cockpit lives at
`apps/operator-cockpit/`. The public Netlify website remains in `dashboard/`
and is not the M7 cockpit surface.

M7 should no longer be tracked as "Not started." Git history shows the local
operator dashboard landed in `462a624` and received QC fixes in `09a6e91`.
The later website deployment commit `4edeb50` repurposed the visible
`dashboard/` Vite entrypoint for the public Profusion AI website deployed to
Netlify. The M7 closeout decision was to keep `dashboard/` as the public site
and move the operator cockpit into `apps/operator-cockpit/`.

Current M7 lock verification:

- `uv run pytest`: 182 passed
- `uv run profusion smoke --offline`: passed
- `cd apps/operator-cockpit && corepack pnpm test`: 3 files passed, 9 tests passed
- `cd apps/operator-cockpit && corepack pnpm lint`: passed
- `cd apps/operator-cockpit && corepack pnpm build:netlify`: exported 2 static item snapshots and built successfully
- `cd apps/operator-cockpit && corepack pnpm smoke:static`: passed for 2 items

M7 browser smoke is documented in
`docs/milestones/M7_OPERATOR_COCKPIT_SMOKE_2026-05-03.md`.
M7 closeout is documented in
`docs/milestones/M7_OPERATOR_COCKPIT_CLOSEOUT_2026-05-03.md`.
M7 final lock is documented in
`docs/milestones/M7_OPERATOR_COCKPIT_FINAL_CLOSEOUT_2026-05-06.md`.

M7.5 reviewer evidence now has a file-first draft `content_video_receipt`
generator, CLI surface, read API, static cockpit snapshot, and one explicit
demo packet visible in the receipt-aware Netlify draft. This keeps evidence
packets outside SQLite while the receipt format is still being validated.

M7.5 first-slice smoke is documented in
`docs/milestones/M7_5_REVIEWER_EVIDENCE_SMOKE_2026-05-03.md`.

## Current Milestone: M6 — Agent-hardening: runbooks, idempotency, handoff — COMPLETE

### M6: Agent Hardening + Operational Durability — COMPLETE

**Goal:** Make Profusion inspectable, retryable, handoff-safe, and durable under repeated operator/agent use without normal recovery requiring direct SQLite edits.

**Checklist:**
- [x] SQLite schema v4: retry lineage and diagnostics metadata on `publish_jobs` and `render_jobs`
- [x] `status --json` plus M6 inspection surfaces: `inspect`, `jobs`, `renders`, `approvals`, `logs`, and `handoff`
- [x] `inspect` reports lifecycle state, latest artifacts/jobs/approval/publish state, blockage, retryability, logs, and exact next safe command
- [x] Conservative `retry` command for failed scheduled publish jobs, immediate publish retry attempts, failed render resubmission, and explicit QA retry
- [x] Schedule duplicate-target guard with `--force` escape hatch
- [x] Redacted diagnostic log helper with paired human-readable and JSON metadata logs
- [x] `smoke --offline` fixture-backed command for local operator-surface verification without live vendors
- [x] Runbooks split into daily operations, failure recovery, handoff, and release checklist docs
- [x] M7/M8 roadmap correction recorded: M7 operator cockpit, M8 measurement loops
- [x] 171/171 tests passing

**Notes:**
- M6 does not add new media generation capability. It turns the command layer into an operator contract layer for future dashboard work.
- Retry preserves failed history. Completed render and publish jobs remain immutable.
- JSON read models are generated by orchestrator services; the future dashboard should not query SQLite directly.
- M7 code landed after M6 and should be reconciled as implemented/displaced,
  not as unstarted.

### M7: Internal Operator Cockpit — COMPLETE

**Goal:** Provide a localhost React operator UI over the existing Profusion
read models and safe action endpoints.

**Repo-truth status:**
- `462a624` implemented the FastAPI backend plus React/Vite local operator dashboard.
- `09a6e91` added M7 QC fixes for SPA fallback, blockage retry CTA, approval timestamp handling, and status validation.
- `4edeb50` deployed the public Profusion AI website and changed the active `dashboard/` Vite entrypoint to the public site.
- `apps/operator-cockpit/` now restores the operator cockpit as a standalone internal Vite app.
- The cockpit consumes existing FastAPI/read-model contracts and does not query SQLite directly.
- Safe retry actions remain guarded; approve, schedule, and publish are displayed as terminal commands rather than broad cockpit buttons.
- Queue, item detail, jobs/failures, artifacts, approvals, logs, handoff, and next safe command views are present.

**Smoke path:**
- API: `uv run profusion serve --dev`
- Cockpit: `cd apps/operator-cockpit && corepack pnpm dev --host 127.0.0.1`
- Local URL: `http://127.0.0.1:5174/`
- Smoke note: `docs/milestones/M7_OPERATOR_COCKPIT_SMOKE_2026-05-03.md`
- Final lock note: `docs/milestones/M7_OPERATOR_COCKPIT_FINAL_CLOSEOUT_2026-05-06.md`

### M7.5: Reviewer Evidence Packets — FIRST SLICE COMPLETE

**Goal:** Generate a reviewer-readable evidence packet from an existing
Profusion content workflow without turning the cockpit into a customer portal
or launching Work Trust prematurely.

**Implemented first slice:**
- `src/orchestrator/receipts/` contains the file-first
  `content_video_receipt` generator.
- `uv run profusion receipt draft --item-id <id> --json` writes packet files to
  `data/receipts/<receipt_id>/`.
- `uv run profusion receipt list --item-id <id> --json` lists generated packet
  summaries for a content item.
- `GET /api/items/{item_id}/receipts` exposes the receipt summaries through the
  existing read-model/API boundary.
- The cockpit static snapshot exporter writes
  `api/items/<id>/receipts.json`.
- The cockpit Evidence page now shows receipt draft presence or the terminal
  draft command for evidence-ready items.
- `scripts/seed-m7-5-receipt-demo.py` creates or reuses an explicit
  `source=m7.5-demo-receipt` approved item for static cockpit preview builds.
- `cd apps/operator-cockpit && corepack pnpm smoke:static` verifies the static
  snapshot and, with `COCKPIT_BASE_URL`, the remote draft routes.
- Generated draft packets include `receipt.md`, `summary.md`,
  `limitations.md`, `reviewer_notes.md`, and `evidence.json`.
- Receipt status starts at `draft` and remains separate from content approval.
- The first receipt boundary is media-trust only: workflow evidence, not
  universal synthetic-media detection, identity verification, liveness
  verification, or proof of no outside manipulation.

**Current draft:**

```text
https://69f770eec1439a4cd6b2a8c9--profusionai.netlify.app/queue
```

The draft is still a static Vite preview with read-only `/api/*` JSON files.
It does not run local FastAPI/SQLite mutations.

The small receipt lifecycle command exists:

- `uv run profusion receipt transition --receipt-id <id> --to reviewed --json`
- `uv run profusion receipt transition --receipt-id <id> --to approved_for_packet --json`
- `uv run profusion receipt transition --receipt-id <id> --to delivered --json`

### M8: Workflow Outcome Observations — FIRST SLICE STARTED

**Goal:** Close the published-output feedback loop without adding platform
automation, optimization algorithms, or customer-facing measurement surfaces.

**2026-05-17 split note:** After the Profusion / Attention Media Lab split,
M8 remains in Profusion only as generic workflow outcome observations. The
earlier Substack and education-channel material is historical context or a
sanitized receipt-demo source, not Profusion's operating direction.

**Plain-English definition:** Manual observation means a human operator records
what happened after a content item was published, reviewed, or used in a
controlled demo. The human observation is the source of truth; Profusion stores
it as a file-first measurement artifact and exposes it through CLI, API, and
cockpit read models.

**Implemented first slice:**
- `src/orchestrator/measurements.py` records file-first manual measurement
  observations under `data/measurements/<item_id>/`.
- `uv run profusion measure record --item-id <id> --platform <slug> --observation-type <slug> --recorded-by <operator> --json`
  writes one observation and transitions eligible `published` items to
  `measured`.
- `uv run profusion measure list --item-id <id> --json` lists item-level
  observations.
- `uv run profusion measure summary --json` exposes aggregate measurement
  counts, platform/type summaries, and simple aggregate metrics.
- `GET /api/items/{item_id}/measurements` and
  `GET /api/measurements/summary` expose the same read models to the cockpit.
- The standalone operator cockpit now has `/measurements` visibility and item
  measurement facts, with static snapshot export support.
- Manual observations can carry generic comparison dimensions for `hook_variant`,
  `content_format`, and `editorial_pillar`.
- Measurement summaries now aggregate comparison rows across hook variants,
  content formats, and editorial pillars without adding platform imports or
  optimization logic.

**Current M8 boundary after the split:**
- Manual/file-first observations only.
- No platform API imports.
- No automated optimization loop.
- No model feedback loop.
- No Work Trust reinterpretation of M8.
- No public website changes.
- No education/content-channel ownership inside Profusion.

**Historical/sanitized demo context: Substack publishing + evidence loop**

This earlier M8 proof path is now historical context or a sanitized demo
receipt source:

```text
Profusion content item -> Substack-ready package -> human approval ->
published Substack post -> RSS readback verification -> receipt/evidence
packet -> manual M8 measurement observation
```

Implemented safe first slice:

- `src/orchestrator/adapters/substack.py` defines typed Substack adapter
  primitives for manual package artifacts and public RSS readback.
- `src/orchestrator/substack_publish.py` prepares file-first Substack package
  artifacts under `data/substack/<item_id>/`.
- `uv run profusion substack package --item-id <id> --json` writes
  `draft.md`, `draft.html`, `metadata.json`, `approval_snapshot.json`,
  `limitations.md`, and `publish_attempts.json`.
- `uv run profusion substack draft --item-id <id> --mode manual --json`
  reuses an existing package when present, instead of overwriting operator
  metadata.
- `uv run profusion substack import-article --draft-path <path> --title <title> --approved-by <operator> --confirm-content-approval --json`
  gives the operator a governed manual path from a human-approved local
  long-form draft to an approved, package-ready Profusion content item without
  editing SQLite by hand.
- `uv run profusion substack publish --item-id <id> --url <published_url> --confirm-publish --json`
  requires an existing package, records that a human intentionally published
  the prepared post, and stores the Substack URL in the existing publish-job
  ledger.
- `uv run profusion substack verify --item-id <id> --url <published_url> --method rss --json`
  requires that the same URL was recorded first, writes public RSS readback
  evidence to `data/substack/<item_id>/verification_rss.json`, and fails if the
  URL is not present in the feed.

Deferred until the manual path proves useful:

- browser automation for draft creation
- browser automation for publishing
- Stackhooks structured readback
- MCP wrapper tools around the Profusion CLI
- automated platform analytics import or optimization

M8.1 voice/manual publication run artifacts existed locally before the split
and now belong to `/home/kyle/attention-media-lab` if used operationally:

- Source PDF text extraction:
  `data/source_packs/profusion_substack_voice/ai_speed_is_not_enough_work_has_to_become_reviewable.md`
- Source pack note:
  `data/source_packs/profusion_substack_voice/README.md`
- Voice guide:
  `data/style/profusion_substack_voice_2026-05-08.md`
- Draft for human review:
  `data/drafts/profusion_substack_voice/human_in_the_loop_is_not_enough.md`

This draft has not been treated as live-publish approved yet. The next operator
gate is Kyle review, then `substack import-article`, then the existing package
-> manual publish -> URL record -> RSS verify -> receipt -> measurement loop.

**Earlier M8 measurement/cockpit verification baseline:**
- `uv run pytest`: 189 passed
- `uv run profusion smoke --offline`: passed
- `cd apps/operator-cockpit && corepack pnpm test`: 4 files passed, 11 tests passed
- `cd apps/operator-cockpit && corepack pnpm lint`: passed
- `cd apps/operator-cockpit && corepack pnpm build:netlify`: exported 2 static item snapshots and built successfully
- `cd apps/operator-cockpit && corepack pnpm smoke:static`: passed for 2 items
- `cd dashboard && corepack pnpm lint`: passed
- `cd dashboard && corepack pnpm build`: passed
- `git diff --check`: passed

**Substack spike QC verification:**
- `uv run pytest tests/test_substack_publish.py -q`: 17 passed
- `uv run pytest tests/test_substack_publish.py tests/test_receipts.py tests/test_measurements.py -q`: 31 passed
- `uv run pytest -q`: 206 passed
- `uv run profusion smoke --offline`: passed
- `git diff --check`: passed

The full baseline still needs to be rerun after the first real Substack
operator scenario before M8 is treated as closed.

**M8 remains open until:**
- at least three manual measurement observations exist
- at least two comparison dimensions have been exercised
- `measure list` and `measure summary` have been reviewed against real/demo data
- `/measurements` has been visually QA'd with non-empty measurement data
- an operator runbook or closeout note documents the end-to-end loop
- the full verification baseline has been rerun

### M5: Scheduling + cross-posting — COMPLETE

**Goal:** Support timed distribution and broader platform coordination without bypassing approval gates.

**Checklist:**
- [x] SQLite schema v3: scheduling metadata added to `publish_jobs` (`account_id`, title/description, `scheduled_for`, platform metadata, last error, attempts, updated timestamp)
- [x] `record_schedule_created` — atomic `approved → scheduled` transition plus one-or-many scheduled publish jobs
- [x] `get_due_publish_jobs`, `mark_publish_job_processing`, `mark_publish_job_failed`, and `record_scheduled_publish_complete`
- [x] `profusion schedule --item-id <id> --at <iso-datetime> --target <platform:account-id>` supports repeatable targets for cross-posting
- [x] `profusion publish-due [--now <iso-datetime>] [--limit N]` publishes due scheduled jobs using the existing V2 adapter
- [x] Scheduled items remain visibly `scheduled` until all scheduled target jobs complete
- [x] Partial cross-post failure leaves the item `scheduled`, marks only the failed job `failed`, and records `last_error`
- [x] 8 schedule CLI tests added; 138/138 tests passing

**Notes:**
- M5 uses local scheduling in Profusion rather than delegating future scheduling to PostBridge. `publish-due` is the explicit due-runner.
- Immediate M4 `profusion publish` still uses `record_publish_complete` and does not leave `scheduled` as a resting state.
- Scheduled publishing uses separate DB helpers so `scheduled` is a real operator-visible state for M5.

---

### M4: Publishing — COMPLETE

**Goal:** Publish approved artifacts via MoneyPrinterV2/PostBridge, preserving approval-gated control.

**Checklist:**
- [x] `POST_BRIDGE_API_KEY` added to config and `.env.example`
- [x] `src/orchestrator/adapters/v2.py` — typed PostBridge publisher: `PublishProfile`, `PublishResult`, `V2PublishError`, `publish(video_path, profile, *, client)`; `client` param enables clean unit testing
- [x] Publish package validated before adapter call: approved status, completed render job, non-empty MP4, `manifest.json`, `qa_report.json`, approval record with `approved` decision
- [x] `insert_publish_job` / `update_publish_job` / `get_publish_jobs_for_item` DB helpers
- [x] `record_publish_complete` — atomic `approved → scheduled → published` + job update in one transaction
- [x] `profusion publish --item-id <id> --platform <slug> --account-id <pb-id>` — creates pending job, calls adapter, atomically transitions on success
- [x] Publishing failures leave item `approved` and mark job `failed` — safe to retry
- [x] 8 V2 adapter unit tests + 12 publish CLI tests = 20 new tests
- [x] 130/130 tests passing (110 M3 + 20 M4)

**Notes:**
- V2 publish path is PostBridge REST API (not Selenium YouTube): upload_media → create_post. No browser session required.
- For immediate publishing only, `approved → scheduled → published` happens atomically in `record_publish_complete`; `scheduled` is not an externally observable resting state on that path.
- The adapter's `client` parameter accepts any `httpx.Client`-compatible mock for testing without live HTTP.

---

### M3: QA + Approval Gates — COMPLETE

**Goal:** Prevent rendered artifacts from advancing to publishing without editorial and human review.

**Checklist:**
- [x] `editorial_risk.md` prompt updated for post-render artifact review context
- [x] `QAResult` Pydantic model validates Claude QA output before state advance
- [x] `editorial.QACheckError` + `run_qa_check()` — all adapter/network failures surface as CLI errors, leave item `rendered`
- [x] `get_completed_render_job_for_item` — joins `render_jobs` through `script_variants` to find the latest completed job
- [x] `record_approval_decision` — single atomic transaction: validates precondition, inserts audit record, walks `qa_passed → awaiting_approval → final` in one commit
- [x] `profusion qa --item-id <id>` — cross-checks all 4 manifest fields against DB, decodes brief JSON fields, writes `qa_report.json` before state advance
- [x] `profusion approve --item-id <id> [--decision approved|rejected|revision_requested]` — delegates entirely to `record_approval_decision`
- [x] `awaiting_approval → scripted` transition added to state machine for `revision_requested`
- [x] 110/110 tests passing (86 M2 + 16 qa CLI + 8 approve CLI)

---

**Notes:**
- QA report is written to `data/renders/<task_id>/qa_report.json` before any state transition; write failure leaves item `rendered`.
- All exception types (including bare `Exception` from network/API failures) leave item `rendered` rather than advancing or crashing.
- Approval is a single DB transaction — `awaiting_approval` is never an externally observable resting state.

---

### M2: Rendering — COMPLETE

**Goal:** Turn a selected scripted variant into a local MP4 artifact via MoneyPrinterTurbo.

**Checklist:**
- [x] `TURBO_URL` added to config and `.env.example`
- [x] `turbo.py` adapter: typed HTTP, Pydantic response validation, state-integer normalization
- [x] Turbo `video_script` + `video_terms` supplied to bypass Turbo's LLM (Claude is the only LLM)
- [x] `profusion render --item-id <id> [--variant name] [--wait] [--max-polls N] [--force]`
- [x] `render_jobs` row created before polling; Turbo task_id used as primary key (no schema bump)
- [x] `--wait` polls until completed/failed; `--max-polls` timeout guard
- [x] Duplicate-job guard (skips if pending/processing job exists unless `--force`)
- [x] Local MP4 downloaded to `data/renders/<task_id>/final.mp4` before state transition
- [x] Render manifest written to `data/renders/<task_id>/manifest.json`
- [x] Item stays `scripted` on any failure; render job marked `failed` with log
- [x] `check-env` probes `GET {TURBO_URL}/api/v1/tasks` (real mounted route)
- [x] 81/81 tests passing (55 M1 + 10 render CLI + 16 turbo adapter unit tests)

**Notes:**
- `_parse_render_response` / `_parse_task_response` are testable pure functions that validate vendor JSON; HTTP transport is separate.
- Transition to `rendered` only occurs after `final.mp4` exists locally and is non-empty.
- Detailed failure logs written to `data/logs/render-<task_id>.log` on any error path.

---

### M1: Editorial Pipeline — COMPLETE

**Goal:** Move content from raw topic → structured brief → script variants, with claims/risks captured and the state machine enforced throughout.

**Checklist:**
- [x] SQLite schema v2: `source_documents` table + `risk_flags` / `source_refs` on `content_briefs`
- [x] v1 → v2 migration is idempotent and preserves legacy rows (tested)
- [x] `profusion ingest --topic` and `--file CSV` create `idea`-stage items
- [x] `profusion plan --item-id <id>` generates a Claude-validated brief, transitions `idea → planned`
- [x] `profusion script --item-id <id>` generates 3 validated variants, transitions `planned → scripted`
- [x] Pydantic draft models validate all Claude output before DB insert (`BriefDraft`, `ScriptBatch`, `RiskFlag`, `ClaimToVerify`, `SourceRef`)
- [x] Prompts seeded under `src/orchestrator/prompts/{planning,scripting,qa}/`
- [x] `profusion status --status <state>` supports lifecycle filtering
- [x] All state transitions go through the `state.transition` guard — invalid transitions surface as CLI errors
- [x] 55/55 tests passing, no live API key required

**Notes:**
- Claude output is parsed as JSON (bare or markdown-fenced) and validated via Pydantic. Failures do not advance state.
- `data/topics/example.csv` seeded as an ingest smoke-test fixture.
- Firecrawl remains stubbed. `source_documents` can be populated manually (CLI surface deferred until scraping is required).

---

### M0: Foundation (Zero to One) — COMPLETE

**Goal:** Durable editorial substrate — repo, state machine, storage, CLI skeleton, vendor submodules.

**Checklist:**
- [x] `uv sync` succeeds from clean clone (Python 3.11.14)
- [x] `uv run python --version` prints 3.11.14
- [x] `uv run profusion status` prints empty queue table
- [x] `uv run profusion check-env` reports ffmpeg present, NVENC not in this ffmpeg build (software encoding fallback active), API key and Firecrawl awaiting .env
- [x] `uv run pytest` — 18/18 passed
- [x] Both vendor submodules present and pinned (Turbo SHA pinned, V2 SHA pinned)
- [x] DECISIONS.md populated with 7 architecture decisions
- [x] THIRD_PARTY_LICENSES.md complete with AGPL-3.0 legal note

**Notes:**
- NVENC not compiled into the system ffmpeg build. Software x264 encoding will be used for now. A custom ffmpeg build with NVENC support can be swapped in later if render speed becomes a bottleneck.
- Firecrawl `/health` returns 404 — service is running but health endpoint path differs. Firecrawl ingest stub ready for M1.

---

## Milestone Roadmap

| Milestone | Focus | Status |
|-----------|-------|--------|
| M0 | Foundation: repo, state machine, DB, CLI skeleton | Complete |
| M1 | Editorial pipeline: topic intake, briefs, scripts | Complete |
| M2 | Rendering: MoneyPrinterTurbo integration | Complete |
| M3 | QA + approval gates | Complete |
| M4 | Publishing: MoneyPrinterV2 integration | Complete |
| M5 | Scheduling + cross-posting | Complete |
| M6 | Agent-hardening: runbooks, idempotency, handoff | Complete |
| M7 | Internal operator cockpit | Complete |
| M7.5 | Reviewer evidence packets | First slice complete |
| M8 | Measurement + learning loops | First slice started |
