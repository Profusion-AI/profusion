# Profusion Pipeline Status

## Current Milestone: M6 — Agent-hardening: runbooks, idempotency, handoff — COMPLETE

### M6: Agent Hardening + Operational Durability — COMPLETE

**Goal:** Make Profusion inspectable, retryable, handoff-safe, and durable under repeated operator/agent use without normal recovery requiring direct SQLite edits.

**Checklist:**
- [x] SQLite schema v4: retry lineage and diagnostics metadata on `publish_jobs` and `render_jobs`
- [x] `status --json` plus M6 inspection surfaces: `inspect`, `jobs`, `renders`, `approvals`, `logs`, and `handoff`
- [x] `inspect` reports lifecycle state, latest artifacts/jobs/approval/publish state, blockage, retryability, logs, and exact next safe command
- [x] Conservative `retry` command for failed scheduled publish jobs, immediate publish retry attempts, failed render resubmission, and explicit QA retry
- [x] Schedule duplicate-target guard with `--force` escape hatch
- [x] Redacted diagnostic log helper with paired human-readable and JSON metadata logs
- [x] `smoke --offline` fixture-backed command for local operator-surface verification without live vendors
- [x] Runbooks split into daily operations, failure recovery, handoff, and release checklist docs
- [x] M7/M8 roadmap correction recorded: M7 localhost dashboard, M8 measurement loops
- [x] 147/147 tests passing

**Notes:**
- M6 does not add new media generation capability. It turns the command layer into an operator contract layer for future dashboard work.
- Retry preserves failed history. Completed render and publish jobs remain immutable.
- JSON read models are generated by orchestrator services; the future dashboard should not query SQLite directly.

### M5: Scheduling + cross-posting — COMPLETE

**Goal:** Support timed distribution and broader platform coordination without bypassing approval gates.

**Checklist:**
- [x] SQLite schema v3: scheduling metadata added to `publish_jobs` (`account_id`, title/description, `scheduled_for`, platform metadata, last error, attempts, updated timestamp)
- [x] `record_schedule_created` — atomic `approved → scheduled` transition plus one-or-many scheduled publish jobs
- [x] `get_due_publish_jobs`, `mark_publish_job_processing`, `mark_publish_job_failed`, and `record_scheduled_publish_complete`
- [x] `profusion schedule --item-id <id> --at <iso-datetime> --target <platform:account-id>` supports repeatable targets for cross-posting
- [x] `profusion publish-due [--now <iso-datetime>] [--limit N]` publishes due scheduled jobs using the existing V2 adapter
- [x] Scheduled items remain visibly `scheduled` until all scheduled target jobs complete
- [x] Partial cross-post failure leaves the item `scheduled`, marks only the failed job `failed`, and records `last_error`
- [x] 8 schedule CLI tests added; 138/138 tests passing

**Notes:**
- M5 uses local scheduling in Profusion rather than delegating future scheduling to PostBridge. `publish-due` is the explicit due-runner.
- Immediate M4 `profusion publish` still uses `record_publish_complete` and does not leave `scheduled` as a resting state.
- Scheduled publishing uses separate DB helpers so `scheduled` is a real operator-visible state for M5.

---

### M4: Publishing — COMPLETE

**Goal:** Publish approved artifacts via MoneyPrinterV2/PostBridge, preserving approval-gated control.

**Checklist:**
- [x] `POST_BRIDGE_API_KEY` added to config and `.env.example`
- [x] `src/orchestrator/adapters/v2.py` — typed PostBridge publisher: `PublishProfile`, `PublishResult`, `V2PublishError`, `publish(video_path, profile, *, client)`; `client` param enables clean unit testing
- [x] Publish package validated before adapter call: approved status, completed render job, non-empty MP4, `manifest.json`, `qa_report.json`, approval record with `approved` decision
- [x] `insert_publish_job` / `update_publish_job` / `get_publish_jobs_for_item` DB helpers
- [x] `record_publish_complete` — atomic `approved → scheduled → published` + job update in one transaction
- [x] `profusion publish --item-id <id> --platform <slug> --account-id <pb-id>` — creates pending job, calls adapter, atomically transitions on success
- [x] Publishing failures leave item `approved` and mark job `failed` — safe to retry
- [x] 8 V2 adapter unit tests + 12 publish CLI tests = 20 new tests
- [x] 130/130 tests passing (110 M3 + 20 M4)

**Notes:**
- V2 publish path is PostBridge REST API (not Selenium YouTube): upload_media → create_post. No browser session required.
- For immediate publishing only, `approved → scheduled → published` happens atomically in `record_publish_complete`; `scheduled` is not an externally observable resting state on that path.
- The adapter's `client` parameter accepts any `httpx.Client`-compatible mock for testing without live HTTP.

---

### M3: QA + Approval Gates — COMPLETE

**Goal:** Prevent rendered artifacts from advancing to publishing without editorial and human review.

**Checklist:**
- [x] `editorial_risk.md` prompt updated for post-render artifact review context
- [x] `QAResult` Pydantic model validates Claude QA output before state advance
- [x] `editorial.QACheckError` + `run_qa_check()` — all adapter/network failures surface as CLI errors, leave item `rendered`
- [x] `get_completed_render_job_for_item` — joins `render_jobs` through `script_variants` to find the latest completed job
- [x] `record_approval_decision` — single atomic transaction: validates precondition, inserts audit record, walks `qa_passed → awaiting_approval → final` in one commit
- [x] `profusion qa --item-id <id>` — cross-checks all 4 manifest fields against DB, decodes brief JSON fields, writes `qa_report.json` before state advance
- [x] `profusion approve --item-id <id> [--decision approved|rejected|revision_requested]` — delegates entirely to `record_approval_decision`
- [x] `awaiting_approval → scripted` transition added to state machine for `revision_requested`
- [x] 110/110 tests passing (86 M2 + 16 qa CLI + 8 approve CLI)

---

**Notes:**
- QA report is written to `data/renders/<task_id>/qa_report.json` before any state transition; write failure leaves item `rendered`.
- All exception types (including bare `Exception` from network/API failures) leave item `rendered` rather than advancing or crashing.
- Approval is a single DB transaction — `awaiting_approval` is never an externally observable resting state.

---

### M2: Rendering — COMPLETE

**Goal:** Turn a selected scripted variant into a local MP4 artifact via MoneyPrinterTurbo.

**Checklist:**
- [x] `TURBO_URL` added to config and `.env.example`
- [x] `turbo.py` adapter: typed HTTP, Pydantic response validation, state-integer normalization
- [x] Turbo `video_script` + `video_terms` supplied to bypass Turbo's LLM (Claude is the only LLM)
- [x] `profusion render --item-id <id> [--variant name] [--wait] [--max-polls N] [--force]`
- [x] `render_jobs` row created before polling; Turbo task_id used as primary key (no schema bump)
- [x] `--wait` polls until completed/failed; `--max-polls` timeout guard
- [x] Duplicate-job guard (skips if pending/processing job exists unless `--force`)
- [x] Local MP4 downloaded to `data/renders/<task_id>/final.mp4` before state transition
- [x] Render manifest written to `data/renders/<task_id>/manifest.json`
- [x] Item stays `scripted` on any failure; render job marked `failed` with log
- [x] `check-env` probes `GET {TURBO_URL}/api/v1/tasks` (real mounted route)
- [x] 81/81 tests passing (55 M1 + 10 render CLI + 16 turbo adapter unit tests)

**Notes:**
- `_parse_render_response` / `_parse_task_response` are testable pure functions that validate vendor JSON; HTTP transport is separate.
- Transition to `rendered` only occurs after `final.mp4` exists locally and is non-empty.
- Detailed failure logs written to `data/logs/render-<task_id>.log` on any error path.

---

### M1: Editorial Pipeline — COMPLETE

**Goal:** Move content from raw topic → structured brief → script variants, with claims/risks captured and the state machine enforced throughout.

**Checklist:**
- [x] SQLite schema v2: `source_documents` table + `risk_flags` / `source_refs` on `content_briefs`
- [x] v1 → v2 migration is idempotent and preserves legacy rows (tested)
- [x] `profusion ingest --topic` and `--file CSV` create `idea`-stage items
- [x] `profusion plan --item-id <id>` generates a Claude-validated brief, transitions `idea → planned`
- [x] `profusion script --item-id <id>` generates 3 validated variants, transitions `planned → scripted`
- [x] Pydantic draft models validate all Claude output before DB insert (`BriefDraft`, `ScriptBatch`, `RiskFlag`, `ClaimToVerify`, `SourceRef`)
- [x] Prompts seeded under `src/orchestrator/prompts/{planning,scripting,qa}/`
- [x] `profusion status --status <state>` supports lifecycle filtering
- [x] All state transitions go through the `state.transition` guard — invalid transitions surface as CLI errors
- [x] 55/55 tests passing, no live API key required

**Notes:**
- Claude output is parsed as JSON (bare or markdown-fenced) and validated via Pydantic. Failures do not advance state.
- `data/topics/example.csv` seeded as an ingest smoke-test fixture.
- Firecrawl remains stubbed. `source_documents` can be populated manually (CLI surface deferred until scraping is required).

---

### M0: Foundation (Zero to One) — COMPLETE

**Goal:** Durable editorial substrate — repo, state machine, storage, CLI skeleton, vendor submodules.

**Checklist:**
- [x] `uv sync` succeeds from clean clone (Python 3.11.14)
- [x] `uv run python --version` prints 3.11.14
- [x] `uv run profusion status` prints empty queue table
- [x] `uv run profusion check-env` reports ffmpeg present, NVENC not in this ffmpeg build (software encoding fallback active), API key and Firecrawl awaiting .env
- [x] `uv run pytest` — 18/18 passed
- [x] Both vendor submodules present and pinned (Turbo SHA pinned, V2 SHA pinned)
- [x] DECISIONS.md populated with 7 architecture decisions
- [x] THIRD_PARTY_LICENSES.md complete with AGPL-3.0 legal note

**Notes:**
- NVENC not compiled into the system ffmpeg build. Software x264 encoding will be used for now. A custom ffmpeg build with NVENC support can be swapped in later if render speed becomes a bottleneck.
- Firecrawl `/health` returns 404 — service is running but health endpoint path differs. Firecrawl ingest stub ready for M1.

---

## Milestone Roadmap

| Milestone | Focus | Status |
|-----------|-------|--------|
| M0 | Foundation: repo, state machine, DB, CLI skeleton | Complete |
| M1 | Editorial pipeline: topic intake, briefs, scripts | Complete |
| M2 | Rendering: MoneyPrinterTurbo integration | Complete |
| M3 | QA + approval gates | Complete |
| M4 | Publishing: MoneyPrinterV2 integration | Complete |
| M5 | Scheduling + cross-posting | Complete |
| M6 | Agent-hardening: runbooks, idempotency, handoff | Complete |
| M7 | Local operator dashboard | Not started |
| M8 | Measurement + learning loops | Not started |
